PRs are welcome. Ruleset in force on master:

- Require linear history (Prevent merge commits from being pushed to matching refs.)
- Block force pushes (Prevent users with push access from force pushing to refs.)
